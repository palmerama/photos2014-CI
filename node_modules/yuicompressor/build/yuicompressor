#!/usr/bin/env node
var args = process.argv.slice(2);  
args.unshift(__dirname + '/yuicompressor*.jar');
args.unshift('-jar');
var exec = require('child_process').exec;
var child = exec('java ' + args.join(' '), function( error, stdout, stderr){ 
if ( error != null ) { console.log(stderr); } 
});